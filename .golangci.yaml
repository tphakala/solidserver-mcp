version: "2"
output:
  sort-order:
    - linter
    - severity
    - file
linters:
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - containedctx
    - contextcheck
    - copyloopvar
    - decorder
    - durationcheck
    - errchkjson
    - errname
    - errorlint
    - exhaustive
    - fatcontext
    - forcetypeassert
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - iface
    - ineffassign
    - makezero
    - misspell
    - mnd
    #- modernize  # disabled - using ruleguard for Go 1.24+/1.25+ modernization instead
    - musttag
    - nakedret
    - nilerr
    - nilnesserr
    - nilnil
    - noctx
    #- nolintlint
    - prealloc
    - predeclared
    - recvcheck
    - revive
    - sloglint
    - staticcheck
    - testifylint
    - thelper
    - unconvert
    - unparam
    - usestdlibvars
    - usetesting
    - wastedassign
  disable:
    - unused
  exclusions:
    rules:
      - path: 'tools/.*\.go'
        linters:
          - dupl
      # The handlers discard the *http.Response that sdsclient's Execute returns,
      # which bodyclose reads as a leaked body. It is not one: the generated
      # client already does io.ReadAll on the real body, closes it, and replaces
      # it with an io.NopCloser over an in-memory buffer before returning (see
      # sdsclient/api_*.go). Closing the returned value again would be a no-op on
      # a NopCloser. These only surfaced once the handlers gained test coverage,
      # because bodyclose traces the value through SSA and needs a caller.
      - path: 'tools/.*_tools\.go'
        linters:
          - bodyclose
      - path: '.*_test\.go'
        linters:
          - goconst
  settings:
    dupl:
      threshold: 120
    gocognit:
      min-complexity: 20
    gocritic:
      disabled-checks:
        - commentFormatting
        - commentedOutCode
      enabled-tags:
        - style
        - diagnostic
        - performance
      settings:
        ruleguard:
          rules: "rules/*.go"
    gocyclo:
      min-complexity: 15
    revive:
      rules:
        - name: unused-parameter
          disabled: true
    gosec:
      # Configure gosec for security scanning
      excludes:
        - G104  # Audit errors not checked (covered by errcheck)
      severity: medium
      confidence: medium
    prealloc:
      # Configure prealloc for slice optimization
      simple: true
      range-loops: true
      for-loops: true
    exhaustive:
      # Check exhaustiveness of enum switch statements
      default-signifies-exhaustive: true
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  uniq-by-line: true
  new: false
